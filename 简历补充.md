# 项目1：PC电商商城

一个面向消费者的美容和营养健康类电商商城。它的业务主要围绕商品销售和会员复购展开。

首先是商品导购。用户可以通过首页、品牌分类或者关键词搜索找到商品，在详情页查看价格、图文介绍、套装、相关推荐、用户评价和商品证照。商城还结合品牌知识、健康类文章和问答，帮助用户了解商品。

第二部分是交易。项目支持游客和登录会员的购物车，也支持直接填写收货信息的快捷下单。购物车结算会涉及商品数量、收货地址、满额赠品、优惠券和运费，并且对藏宝岛这类特殊订单有单独的处理分支。订单创建后接入微信扫码支付入口和支付宝支付，用户之后可以在会员中心查询订单、取消订单以及查看物流。

第三部分是会员经营。项目有 VIP、金卡、钻卡和至尊黑卡等等级展示，支持黑卡季卡和年卡购买，以及生日礼、会员礼包等权益。用户可以查看积分和账户流水，用积分兑换现金券或者参与礼品兑换。其中部分积分入口会发送服务提醒，由健康顾问继续承接。



# 项目2：内部ERP

这个项目是公司内部使用的一套综合 ERP，服务于销售、客服、运营、采购、财务和管理人员。它的核心业务可以概括为：从获取客户，到完成交易和交付，再到经营分析及客户复购。

首先是客户模块。公司通过广告投放、SEO 内容、电商和直播等渠道获取线索，系统将线索纳入客户资源池，再根据归属、排班和分配规则交给销售跟进。销售通过电话、微信、预约回访等方式维护客户，系统记录跟进过程，并管理客户保护期、资源释放和转移。成交之后，还可以通过会员权益、优惠券、生日服务和转介绍等方式促进复购。

第二是订单模块。它承接后台人工开单和平台订单导入，管理商品明细、客户和收货信息，并支持定金、余额、赠品、换货和样品等不同单据场景。订单需要经过处理、条件校验和必要审批，才能进入发货环节，之后继续跟踪物流、签收、到款和业绩归属。出现问题时，还要处理退货、退款、售后跟进以及相应的业绩调整，所以订单模块连接了销售、仓配、财务和售后。

第三是统计模块。它汇集客户、订单、商品、投放和人员等数据，按个人、团队、部门、渠道和时间形成报表与看板。管理人员可以查看业绩达成、客户转化、发货签收、库存情况、推广投入产出，以及电商和直播表现。这里的关键是指标口径：订单金额、回款、签收业绩和财务利润并不是同一个概念，需要分别处理统计时间、人员归属和数据来源。

围绕这三条主线，系统还有产品资料、价格和促销配置，库存预警、补货、采购合同与供应商管理，以及支付渠道、费用审批台账和电商月度核算。内部管理方面覆盖招聘入职、考勤、绩效面谈、学习考试、公告、工单和行政资产，并通过组织权限、文件、日志、钉钉和微信等集成能力支撑各部门协作。





# 项目3：智能问答Agent

基于LangChain 搭建面向公司销售人员的智能问答Agent，结合RAG 检索增强生成与线上代码仓库，为销售业务问题提供信息检索与回答支持。





# 重难点

## 多线程

1. 在ERP订单详情查询场景中，先获取订单主信息，再通过线程池并行查询客户、商品及发货信息，并利用Future获取各任务结果，最终汇总组装后返回完整订单详情，从而减少多个服务串行调用的等待时间。
2. 在我们商城里，有一个文章搜索的功能，用户输入关键字要搜索文章，同时需要保存用户的搜索记录（搜索历史），这块我设计的时候，为了不影响用户的正常搜索，我们采用的异步的方式进行保存的，为了提升性能，我们加入了线程池，也就说在调用异步方法的时候，直接从线程池中获取线程使用



## 限流熔断

1. **商城购物车下单接口：按用户限流，防止连续提交**

**接口：** `POST /order/api/customer/pc/shopping-cart/order`

**原先：** 商城购物车页面调用这个接口创建订单。已检查的网关配置未针对该接口设置独立限流策略，不能单独控制某个用户的连续提交。

**优化：**

- 使用“已认证用户 ID＋接口”作为限流键。
- 初始设置：**每个用户每 10 秒最多提交 2 次**。
- 多实例通过 Redis 原子计数，共享配额。
- 超额返回 HTTP `429`，提示“提交过于频繁，请稍后重试”。
- 用户身份必须由服务端核验，不能直接使用请求体传入的客户 ID。

**优化后的效果：** 用户反复点击或脚本连续提交时，多余请求在进入订单业务前被拦截，减少重复执行订单校验和数据库操作的压力。**限流只能减少连续请求，不能保证订单不重复；后者仍需要幂等。**



2. **商品详情及强制刷新入口：普通访问与刷新分别限流**

**入口：** 商城 `/Product/{productId}.html`，以及携带 `refresh=true` 的请求。

**原先：** 商品详情支持通过 `refresh=true` 删除 MongoDB 中的商品缓存，并触发下游刷新。该控制器的已检查路径没有看到刷新专用的权限和限流控制。

**优化：**

- 普通详情浏览：初始按可信来源 IP 设置**每分钟 120 次，允许短时突发 20 次**，先观察办公网络、代理和正常爬虫的误伤情况。
- 强制刷新：改为受鉴权保护的管理操作。
- 刷新同时设置两层限制：**同一商品每分钟最多刷新 1 次；同一管理员每分钟最多刷新 10 次**。
- 被限制的刷新请求不删除缓存、不调用下游，普通浏览继续读取已有内容。
- IP 从可信代理链解析，不能信任任意客户端传入的转发头。

**优化后的效果：** 限制异常高频访问，尤其防止反复刷新造成“删缓存—重新回源”，保护商品服务。刷新流量与正常浏览不再共用一种处理方式。



## redis主从

1. **ERP：品牌列表、热门品牌查询**

**具体位置：**
[BrandInfoService.cs → GetBrandFromSaleCount() (line 503)](/D:/Projects/ERP/sources/ERP.API/Apis/Ailai.Product/Models/Service/BrandInfoService.cs:503)

**原先：**
已经使用 Redis 缓存品牌查询结果，缓存键为 `Ailai.BrandPY:{shopType}:{py}:v4`，缓存时间为 24 小时，通过 `_cache133` 连接访问。

**增加主从后：**
品牌列表展示优先读从节点；缓存生成、更新、删除走主节点。从节点未命中时回查主节点，尤其避免热门品牌分支因为复制延迟错误返回空列表。

**预期效果：**
将品牌浏览产生的缓存读取分担到从节点，减少主节点读压力。适用于业务允许短暂展示旧品牌信息的情况。



2. **ERP：商品系列列表——改造范围小**

**具体位置：**
[ProductSeriesService.cs → FindAll() (line 25)](/D:/Projects/ERP/sources/ERP.API/Apis/Ailai.Product/Models/Service/ProductSeriesService.cs:25)

**原先：**
查询 `Ailai.ProductSeries` 缓存，未命中则查数据库，并缓存 24 小时。

**增加主从后：**
普通商品系列展示读取从节点；未命中先查主节点，再决定是否查询数据库。缓存写入和失效操作全部走主节点；后台修改后需要立即确认结果的查询仍读主节点。

**预期效果：**
分担商品系列列表的 Redis 读请求，同时保持现有数据库回源逻辑。它的业务边界清晰，比直接改订单流程更适合验证主从读写分离。





## 锁

1. **商品详情缓存并发优化**

**原先是怎样的**

商品详情先查 MongoDB，未命中后进入一个全局 `static lock`，在锁内依次调用商品详情、商品知识、商品问答接口，再写入 MongoDB。

由此产生几个问题：

- 不同商品争用同一把锁，一个商品加载慢，会阻塞其他未命中缓存的商品。
- 单机锁只能保护当前进程，多实例之间仍可能重复重建。
- 等待锁的请求发现缓存已被其他请求写入后，没有将缓存重新赋给当前结果。

源码：[ProductController.cs (line 179)](/D:/myProjects/ShopWeb/Ailai.ShopWeb/Controllers/ProductController.cs:179)

### 大白话说什么问题

就是**顾客逛商城时，点击某个商品，进去看商品介绍的那个页面**。

比如：打开商城 → 点击一款保健品 → 看到商品介绍、相关知识、常见问答。我们讨论的就是这个页面，**不是员工在 ERP 后台录入或修改商品的页面**。

现在的问题，用大白话说：

- 网站已经保存过这个商品的展示资料，打开就比较快。
- 没保存过，就需要临时去其他接口取资料。
- 但现在取资料时，大家共用一个“通行证”：商品 A 正在取资料，商品 B 即使完全不同，也得排队。
- 我们准备改成：**不同商品各取各的；同一个商品来了很多人，只让一个人去取，其他人共享结果。**

这样做的目标是：**多人同时浏览时，减少不必要的排队，也避免重复取同一份资料。**

所以，这个优化应该放在你的**电商项目**里讲，具体就是“商品详情页加载优化”。



### 大白话说如何解决

**最先要做的，就是把“一把大锁”换成“每个商品一把锁”。**

现在相当于：

> 店里只有一个店员。有人问商品 A 的资料，店员去找，问商品 B、C 的人也都得等。

改成这样：

> A、B、C 的资料可以同时找。
> 但如果十个人都问 A，只安排一次查找，找到后一起给这十个人。

具体修改顺序：

1. **先查有没有保存好的商品资料。**
   有就直接返回，跟现在一样。
2. **没资料，就按商品编号安排查找。**
   查 A 不影响查 B；如果已经有人在查 A，后面的请求就等他的结果，不要重复查。
3. **等完以后，把找到的资料返回给每个人。**
   你现在代码里有个遗漏：排队的人发现别人已经把资料存好了，却没把它取出来给当前用户。这一步要补上。
4. **查找失败时结束等待。**
   告诉用户暂时无法加载，并允许下次重新查，不能让大家一直卡着。

**用到的技术：按商品编号加锁、二次读取缓存。** 进一步可以用“请求合并”，让同商品的请求直接共享一次查询结果。

先做好这些，就能解决已经发现的主要问题。**只有以后部署多台服务器时，再用 Redis 分布式锁，让不同服务器也知道“这个商品已经有人在查了”。**

做完同时打开几个不同的未缓存商品，检查它们是否能一起加载；再同时访问同一个商品，检查后台是不是只查了一轮。具体快了多少，要测试后才能写进简历。

