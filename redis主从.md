你问的是：**应用访问 Redis 时，主节点和从节点各干什么，读写请求到底发给谁。** 上次我直接讲同步机制，没把这个前提讲清楚。

我们假设现在有 3 台 Redis：

```text
主节点 A
  ├── 从节点 B
  └── 从节点 C
```

A、B、C 是三个独立运行的 Redis 实例，都有自己的内存。B、C 会复制 A 的数据，所以它们保存的基本上是同一份数据，可能存在短暂延迟。

**一、更新数据：应用应该访问主节点**

比如用户修改昵称：

```text
应用 → 主节点 A：SET name 李四
              ↓
         A 修改自己的数据
              ↓
         将变化同步给 B、C
              ↓
         B、C 修改各自的数据
```

这里的“更新”包括新增、修改、删除，以及计数器加一等所有改变数据的操作。

**从节点默认只读。** 如果应用把 `SET`、`DEL` 等写命令发给从节点，从节点通常会返回 `READONLY` 错误，**不会自动帮你把请求转交给主节点**。

所以，应用需要知道写请求该发到哪里。

**二、查询数据：主节点和从节点都能查，由应用或客户端决定访问谁**

不是说“主节点只能写，从节点只能读”。**主节点能读也能写，从节点默认只能读。**

常见有两种用法：

| 用法 | 查询发给谁 | 更新发给谁 | 特点 |
|---|---|---|---|
| 所有请求都访问主节点 | A | A | 简单，避免读到从节点尚未同步的数据 |
| 读写分离 | B、C，也可以包含 A | A | 分担查询压力，但可能读到旧数据 |

**配置好主从复制，不等于自动开启读写分离。** 请求发给谁，需要应用代码、Redis 客户端或代理来安排。

例如，应用一直连接 A，那么即使有 B、C，所有查询也还是由 A 处理。A 不会因为自己忙，就自动把查询分给 B、C。

**三、为什么查询从节点可能读到旧数据？**

因为主节点更新后，数据传给从节点需要时间，而且默认不会等从节点更新完才返回成功。

假设原来昵称是“张三”：

```text
① 应用向 A 发起更新：改成“李四”
② A 已经改成“李四”，向应用返回成功
③ 应用立刻向 B 查询昵称
④ B 还没收到更新，返回“张三”
⑤ B 收到并执行更新，之后再查才返回“李四”
```

所以：

- 用户刚修改资料，立刻查看结果：通常读主节点更合适。
- 一些允许短暂延迟的查询：可以读从节点，分担主节点的压力。

读主节点能避免这里的**主从复制延迟**问题，但不意味着整个系统就具备了跨故障的强一致性。[Redis 官方复制说明](https://redis.io/docs/latest/operate/oss_and_stack/management/replication/)

**四、从节点是怎么得到数据的？**

可以理解为：**刚加入时先抄一份完整数据，之后主节点有变化，就把变化告诉它。**

比如新启动一个从节点 B：

1. B 主动连接 A，请求同步。
2. A 生成一份数据快照，发给 B。
3. B 加载这份快照，建立自己的数据副本。
4. 同步期间 A 新收到的修改，也会补给 B。
5. 之后 A 持续把数据变化发给 B，B 跟着执行。

因此，从节点查询时，**查的是自己内存里的数据**，不是收到查询后再去主节点查。

如果 B 短暂断线，重连后会尝试补齐遗漏的变化；如果遗漏太多，主节点已经不保留那段历史，就要重新复制完整数据。[Redis 同步机制](https://redis.io/docs/latest/operate/oss_and_stack/management/replication/)

**五、主节点挂了，从节点会自动接管吗？**

**仅仅配置主从复制，不会自动选出新主节点。**

假设 A 挂了：

- 应用继续访问 A，读写都会失败。
- B、C 默认仍然是从节点，不能正常接收写入。
- B、C 是否继续提供旧数据查询，取决于配置。

如果希望自动恢复写入，一般还要配 **Sentinel（哨兵）**：

```text
A 故障
   ↓
哨兵检测并确认故障
   ↓
选择一个从节点，例如 B，提升为主节点
   ↓
C 改为跟随 B
   ↓
应用通过支持哨兵的客户端发现 B，改为向 B 发起写入
```

这个切换需要时间，并且由于复制默认异步，A 故障前已经返回成功、但还没传给从节点的部分写入，可能丢失。[Redis 官方说明](https://redis.io/docs/latest/operate/oss_and_stack/management/replication/)

用一个完整例子串起来，你的应用可以这样运行：

```text
用户修改昵称 ──→ 应用 ──→ 主节点 A
用户查询资料 ──→ 应用 ──→ 主节点 A（希望看到刚修改的结果）
普通列表查询 ──→ 应用 ──→ 从节点 B 或 C（允许短暂延迟）

                            A
                            │ 自动复制数据变化
                         ┌──┴──┐
                         B     C
```

**读写请求由应用或客户端选择节点；主从复制负责把主节点的数据变化同步给从节点。这是两件不同的事。**